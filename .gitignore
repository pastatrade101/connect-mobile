.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
ios/Pods/
ios/.symlinks/
ios/Flutter/ephemeral/
android/.gradle/
android/local.properties
*.iml
.idea/
.DS_Store

# Firebase project identity — supplied per deployment, never committed.
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
lib/firebase_options.dart

# Play upload key — the keystore lives outside the repo, its password never enters it.
android/key.properties
*.jks
*.keystore

# Flutter tool crash dumps: local paths and device ids, never useful to anyone else.
*.log
